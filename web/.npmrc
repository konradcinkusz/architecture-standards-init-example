# Next.js standalone output traces dependencies from a real node_modules tree;
# pnpm's default symlinked store confuses that trace inside a container build.
node-linker=hoisted
strict-peer-dependencies=false
