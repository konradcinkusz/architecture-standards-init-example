{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  },
  "AllowedHosts": "*",

  "//": [
    "No secret is ever a literal in this file (P5). Connection strings arrive as",
    "environment variables — from the AppHost locally, from Fly secrets when",
    "deployed. A committed appsettings with a password in it is the failure this",
    "repository's secret scanner exists to catch."
  ],

  "Cors": {
    "AllowedOrigins": []
  }
}
